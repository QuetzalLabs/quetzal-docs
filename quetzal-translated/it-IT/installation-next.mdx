---
title: "Next.js"
description: "Come installare Quetzal nel tuo progetto Next.js"
---

## Prerequisiti

Quetzal può essere utilizzato per tradurre Next.js che usa il [framework next-intl](https://next-intl-docs.vercel.app/).

## Installazione di Quetzal

```_ Installa
npm install --save @quetzallabs/i18n-next @quetzallabs/i18n-core
```

## Configurazione

Dopo l'installazione del pacchetto, è necessaria una configurazione aggiuntiva.Script">
    Run `npx queEsegui script di configurazionehe direEsegui `npx quetzal-setup` nella directory in cui hai installato il pacchetto Quetzaley">
    Set the environmeImposta Chiave API `QUETZImposta la variabile d'ambiente `QUETZAL_API_KEY` con la tua Chiave API Quetzal. La tua
    Chiave API si trova nella tua [dashboard del progetto](https://getquetzal.com/dashboard/impostazioni/progetto/0)etzal.config.json">
    ThAggiorna quetzal.config.json createLo script di configurazione dovrebbe averlo creato per te. Aggiorna `paths` in modo che punti alle directory con il codice che intendi localizzare. Aggiorna `parser` a `i18next-parser`json
 ```js Esempio quetzal.config.json
{
  "paths": ["src/**/*.{js,tsx}"],
  "parser": "i18next-parser"
}
```xt-intl provider">
  QuetzConfigura il provider next-intlackagQuetzal utilizza il pacchetto `next-intl` per inserire le traduzioni nella tua app,
quindi la tua app deve utilizzare il `NextIntlClientProvider`:

   Apparirà più o meno così:tProvi```js

import { NextIntlClientProvider } from "next-intl";
import { getMessages } from "next-intl/server";

const messages = await getMessages();

...

return (

    <html>
        <body>
            <NextIntlClientProvider messages={messages}>
                {children}
            </NextIntlClientProvider>
        </body>
    </html>

);

```eUlteriori dettagli su questo provider sono disponibili nella [documentazione di next-intl](https://next-intl-docs.vercel.app/docs/getting-started).xt-intl plugin">
    Your Configura il plugin next-intlhe `NexLa tua app deve anche aggiungere il `NextIntlPlugin` da `next-intl` nel tuo file next.config.fig.mjs">
    ```js
    impnext.config.mjsntlPlug```js
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin();

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default withNextIntl(nextConfig);
```g.js">
    ```js
    consnext.config.jstlPlugi```js
const createNextIntlPlugin = require('next-intl/plugin');

const withNextIntl = createNextIntlPlugin();

/\*_ @type {import('next').NextConfig} _/
const nextConfig = {};

module.exports = withNextIntl(nextConfig);

``` on this plugin can be fUlteriori dettagli su questo plugin sono disponibili nella [documentazione di next-intl](https://next-intl-docs.vercel.app/docs/getting-started). done!

Setup should b## E hai finito! cLa configurazione dovrebbe essere completata, e puoi [iniziare a tradurre la tua app](/extension).`n## Verifica la Tua InstallazioneioEsegui `npx quetzal-process-translations` per testare la tua installazione.mages/process-output.png)

If your output looks similar to theSe il tuo output è simile a quello sopra, allora Quetzal è stato installato correttamente e la tua app inizierà a supportare altre lingue!e In caso di errori, si spera che il problema sia chiaro e risolvibile. In caso contrario, [contattaci](https://getquetzal.com/contact) e possiamo far funzionare la tua installazione.il if you Nota: Questo script fallirà se non hai contrassegnato alcun testo nella tua app per
  la traduzione. Quindi, potresti dover [iniziare a tradurre la tua app](/extension)
  primaegrate seQuetzal è progettato per integrarsi perfettamente nel tuo processo di build. Quindi, questo script dovrebbe essere eseguito almeno ogni volta che viene effettuata una build, ma potrebbe essere eseguito con la frequenza desiderata.d Lo script di configurazione dovrebbe aver aggiunto `npx quetzal-process-translations` allo script pre-build nel package.json del tuo progetto, ma questo può essere modificato.